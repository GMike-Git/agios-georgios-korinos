# Ιερά Μονή Αγίου Γεωργίου Κορινού — Τεκμηρίωση Ιστοσελίδας

Στατική ιστοσελίδα (HTML/CSS/JS σε ένα αρχείο `index.html`), με περιεχόμενο (κείμενα/φωτογραφίες) που φορτώνεται δυναμικά από αρχεία `.json`, διαχειρίσιμα μέσω ενσωματωμένου CMS (`/admin`, Sveltia CMS). Χωρίς ανάγκη server ή βάσης δεδομένων· φιλοξενείται σε Netlify, συνδεδεμένο με GitHub.

---

## 1. Δομή αρχείων

```
agios-georgios-korinos/
├── index.html              ← όλος ο κώδικας (HTML + CSS + JavaScript σε ένα αρχείο)
├── content.json              ← όλα τα κείμενα, σε 5 γλώσσες
├── images-fixed.json          ← 13 σταθερές φωτογραφίες (μία θέση η καθεμία)
├── images-gallery.json         ← λίστα φωτογραφιών του Πολυμέσα
├── admin/
│   ├── index.html                ← το CMS (Sveltia)
│   └── config.yml                 ← ορισμός πεδίων/φορμών του CMS
├── images/                          ← οι πραγματικές φωτογραφίες, favicons
└── README.md                          ← οδηγός χρήσης του /admin + αντιμετώπιση προβλημάτων
```

Δεν υπάρχουν εξωτερικά αρχεία `.css` — όλα μέσα στο `<style>` του `index.html`. Δύο `<script>` blocks: ένα για τον Βοηθό Προσκυνητών, ένα για όλα τα υπόλοιπα (rendering περιεχομένου, γλώσσα/θέμα, gallery, panorama tilt, χάρτης).

**Πώς φορτώνεται το περιεχόμενο:** κάθε στοιχείο κειμένου φέρει `data-key="..."`· κάθε φωτογραφία σταθερής θέσης φέρει `data-img-key="..."`. Στη φόρτωση, JavaScript κάνει `fetch()` στα `content.json`/`images-fixed.json`/`images-gallery.json` και γεμίζει τα στοιχεία βάσει αυτών των κλειδιών. Τα ελληνικά (και η αρχική φωτογραφία) παραμένουν ενσωματωμένα κατευθείαν στο HTML ως εφεδρεία — η σελίδα δεν είναι ποτέ κενή, ακόμα κι αν το fetch αποτύχει.

## 2. Ενότητες σελίδας (με σειρά εμφάνισης)

| Ενότητα | id | Περιεχόμενο |
|---|---|---|
| Αρχική (Hero) | `#top` | Κυρίως φωτογραφία, τίτλος, εισαγωγική φράση |
| Ιστορικό | `#history` | Ιστορία μονής, καθολικό, θαυματουργή εικόνα |
| Πανοραμική αυλή | — | Διαδραστική φωτογραφία με 3D κλίση στο ποντίκι/scroll |
| Πολυμέσα | `#gallery` | Πλέγμα φωτογραφιών με μεγέθυνση (lightbox) |
| Πανηγύρια | `#feasts` | 3 γιορτές, "Άναψε ένα κερί" |
| Η Αδελφότητα | `#community` | Καθηγουμένη, μοναχές |
| Επίσκεψη | `#visit` | Οδηγίες πρόσβασης, κανόνες επίσκεψης |
| Δωρεές | `#donate` | Τηλέφωνο επικοινωνίας για υποστήριξη |
| Επικοινωνία | `#contact` | Διεύθυνση, τηλέφωνο, χάρτης |

Το κεντρικό μενού πλοήγησης πάνω-πάνω οδηγεί σε: Αρχική, Ιστορικό, Πολυμέσα, Επικοινωνία, Δωρεές, και το dropdown Ρυθμίσεις.

## 3. Γλώσσες (5)

Ελληνικά (προεπιλογή), Αγγλικά, Σερβικά, Βουλγαρικά, Ρουμανικά.

**Μηχανισμός:** κάθε μεταφρασμένο στοιχείο υπάρχει ως ξεχωριστό HTML element με χαρακτηριστικό `data-lang="el/en/sr/bg/ro"`. Το CSS δείχνει μόνο το element που ταιριάζει με τη γλώσσα που είναι επιλεγμένη στο `<html data-lang="...">`. Η επιλογή γίνεται από τα κουμπιά **GRE / ENG / SRB / BGR / ROU** στις Ρυθμίσεις.

**Σημαντικό για μελλοντικές αλλαγές κειμένου:** κάθε φορά που αλλάζει ή προστίθεται κείμενο, πρέπει να ενημερωθούν **και τα 5** αντίγραφα (ένα ανά γλώσσα), αλλιώς κάποια γλώσσα θα εμφανίζει κενό ή παλιό κείμενο.

## 4. Φόντο / Θέματα (5 παλέτες)

Λευκό, Μαύρο, Θαλασσί (Βυζαντινό μπλε), Πράσινο Ελιάς, Πορφύρα (Βυζαντινή αυτοκρατορική). Επιλέγονται από τα κυκλικά δείγματα χρωμάτων στις Ρυθμίσεις → Φόντο.

Κάθε παλέτα ορίζεται ως ένα block `html[data-theme="..."]{ --bg: ...; --text: ...; ... }` στην αρχή του `<style>`. Όλα τα υπόλοιπα CSS χρησιμοποιούν αυτές τις μεταβλητές (`var(--bg)`, `var(--text)` κ.λπ.), οπότε η προσθήκη νέας παλέτας δεν απαιτεί αλλαγές αλλού στον κώδικα — μόνο ένα νέο block μεταβλητών + ένα νέο κουμπί-δείγμα.

Όλοι οι συνδυασμοί φόντου/κειμένου έχουν ελεγχθεί για επαρκή αντίθεση (WCAG AA, ≥ 4.5:1).

## 5. Βοηθός Προσκυνητών (FAQ widget)

Κουμπί κάτω-δεξιά που ανοίγει πάνελ με προκαθορισμένες ερωτήσεις/απαντήσεις (όχι πραγματικό AI — ντετερμινιστικός οδηγός, 100% ακριβής, χωρίς κόστος λειτουργίας). Οι ερωτήσεις/απαντήσεις ορίζονται στο JavaScript object `assistantData`, με μεταφράσεις και στις 5 γλώσσες.

**Αρχή σχεδιασμού:** ο βοηθός ποτέ δεν απαντά ο ίδιος σε πνευματικά αιτήματα (π.χ. προσευχή) — πάντα παραπέμπει σε πραγματική επικοινωνία με τις αδελφές.

## 6. Άλλα interactive στοιχεία

- **"Άναψε ένα κερί"** — συμβολικό, χωρίς ψεύτικο μετρητή επισκεπτών (σκόπιμη επιλογή, για ειλικρίνεια)
- **Πολυμέσα / lightbox** — κλικ σε φωτογραφία ανοίγει μεγέθυνση, κλείνει με Esc ή κλικ έξω
- **Πανοραμική αυλή** — 3D κλίση με το ποντίκι + parallax στο scroll (απενεργοποιείται αυτόματα σε συσκευές αφής)

## 7. Περιεχόμενο που εκκρεμεί (χρειάζεται πραγματικά στοιχεία από τη μονή)

- **Ώρες επίσκεψης** — προς το παρόν λέει "καλέστε για επιβεβαίωση", καθώς δεν υπάρχει σταθερό ωράριο
- **Προϊόντα μονής** — δεν έχει προστεθεί ακόμα, καθώς δεν δόθηκαν στοιχεία
- **Τραπεζικός λογαριασμός για δωρεές** — δεν έχει προστεθεί, μόνο τηλέφωνο επικοινωνίας

Αυτά τα σημεία **δεν** συμπληρώθηκαν με εικασίες, για να μην υπάρξει λάθος πληροφορία σε ιστοσελίδα πραγματικής μονής. Μπορούν να ενημερωθούν οποτεδήποτε μέσω του `/admin` μόλις υπάρξουν τα πραγματικά στοιχεία.

## 8. SEO / Κοινωνικά δίκτυα

Έχουν προστεθεί: meta description, Open Graph tags (προεπισκόπηση σε Facebook/WhatsApp), Twitter Card, favicons — όλα ενημερωμένα με το πραγματικό domain (`agios-georgios-korinos.netlify.app`).

## 9. Δημοσίευση online — τρέχουσα κατάσταση (ολοκληρωμένη)

- **Hosting:** Netlify, συνδεδεμένο απευθείας με το GitHub repository `GMike-Git/agios-georgios-korinos` — κάθε αλλαγή στο GitHub (είτε μέσω `/admin`, είτε μέσω `git push`) ξαναχτίζει αυτόματα το ζωντανό site.
- **Domain:** προς το παρόν στο δωρεάν `agios-georgios-korinos.netlify.app`. Αν αποκτηθεί ιδιόκτητο domain στο μέλλον, χρειάζεται: (α) σύνδεσή του στο Netlify (Domain settings), (β) ενημέρωση των τεσσάρων σημείων SEO στο `<head>` του `index.html` (αναζήτησε `agios-georgios-korinos.netlify.app` και αντικατέστησέ το με το νέο domain).

## 10. Γενικές οδηγίες συντήρησης

**Για απλές αλλαγές κειμένου/φωτογραφιών** — χρησιμοποίησε το `/admin` (δες `README.md` για πλήρη οδηγό, σύνδεση, και αντιμετώπιση προβλημάτων). Δεν χρειάζεται επαφή με κώδικα.

**Για αλλαγές στη δομή/σχεδίαση της σελίδας** (νέα ενότητα, νέο εφέ, αλλαγή σχεδίασης):
- **Νέο κείμενο:** πρόσθεσε το αντίστοιχο `data-key` στο HTML, και τις 5 μεταφράσεις στο `content.json`. Πρόσθεσε επίσης το αντίστοιχο πεδίο στο `admin/config.yml` ώστε να είναι διαχειρίσιμο μέσω CMS.
- **Νέα σταθερή φωτογραφία:** `data-img-key` στο HTML + entry στο `images-fixed.json` (μέσα στο `"fixed": {...}"`) + πεδίο στο `config.yml` (πρόσεξε: η ένθεση των πεδίων στο `config.yml` πρέπει να ταιριάζει **ακριβώς** με τη δομή του JSON — δες README.md ενότητα 7 για το πρόβλημα που είχαμε εδώ).
- **Νέα ερώτηση στον Βοηθό:** προσθήκη νέου αντικειμένου στο `assistantData` μέσα στο `index.html`, με μεταφράσεις και στις 5 γλώσσες (ο Βοηθός δεν είναι ακόμα συνδεδεμένος με το CMS — παραμένει χειροκίνητη επεξεργασία κώδικα).
- Πριν από κάθε αλλαγή σε μεγάλη κλίμακα, κάνε πρώτα `git pull` (μη χάσεις αλλαγές από το CMS) και δούλεψε τοπικά.

---

*Τεχνολογία: καθαρή HTML/CSS/JavaScript, χωρίς frameworks ή εξαρτήσεις build στο ίδιο το site. Εξωτερικές συνδέσεις: Google Fonts, Google Maps, και το Sveltia CMS (φορτώνεται μόνο μέσα στο `/admin`).*
